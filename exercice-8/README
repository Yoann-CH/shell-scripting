We used the given script to start.After we used two "if".The first is to say if the number given by the user is higher or lower that the number to guess.The second is when the user find the number to say that he succeed.
